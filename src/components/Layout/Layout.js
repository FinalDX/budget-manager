import React from "react";
import Budget from "../../containers/Budget/Budget";

const layout = props => (
  <div>
    <header>
      <nav />
    </header>
    <main>
      <Budget />
    </main>
    <footer />
  </div>
);

export default layout;
